# Model YAML
# Reference documentation: https://docs.rilldata.com/reference/project-files/models
# “City and population data from SimpleMaps World Cities Basic, licensed CC BY 4.0.”

type: model
materialize: true

connector: duckdb
sql: "select * from read_csv('data/worldcities.csv', auto_detect=true, ignore_errors=1, header=true)"