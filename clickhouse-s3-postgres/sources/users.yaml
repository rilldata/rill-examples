type: model

refresh:
  cron: "0 1 * * *"

sql: >
  SELECT * FROM postgresql(
    '{{ .env.postgres_host }}',
    '{{ .env.postgres_database }}',
    'users',
    '{{ .env.postgres_user }}',
    '{{ .env.postgres_password }}'
  )

output:
  type: dictionary
  primary_key: id
