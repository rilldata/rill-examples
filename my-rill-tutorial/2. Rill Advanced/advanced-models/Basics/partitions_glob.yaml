# Replace the path: parameter with your bucket that is partitioned by date

type: model

incremental: true
refresh:
    cron: "0 8 * * *"

partitions:
  glob:
    connector: gcs
    path: gs[s3]://path/to/file/**/*.parquet

sql: SELECT * FROM read_parquet('{{ .partition.uri }}')


