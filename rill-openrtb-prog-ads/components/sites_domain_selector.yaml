# Component YAML
# Reference documentation: https://docs.rilldata.com/reference/project-files/components
    
type: component


output:
  name: sites_domain
  type: string

data:
  metrics_sql: SELECT DISTINCT sites_domain FROM bids limit 10000 

select:
  valueField: "sites_domain"
  placeholder: "Site Domain"