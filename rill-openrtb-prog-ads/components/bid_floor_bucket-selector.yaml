# Component YAML
# Reference documentation: https://docs.rilldata.com/reference/project-files/components
    
type: component


output:
  name: bid_floor_bucket
  type: string

data:
  metrics_sql: SELECT DISTINCT bid_floor_bucket FROM bids limit 10000 

select:
  valueField: "bid_floor_bucket"
  placeholder: "Bid Floor Bucket"