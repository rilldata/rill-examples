# Source YAML
# Reference documentation: https://docs.rilldata.com/reference/project-files/sources

type: model
materialize: true

connector: "duckdb"
sql: |
  select * from read_csv('gs://ws1-listeningparty.rilldata.com/rill_yt_test/all-time/video_mapping.csv', auto_detect=true, ignore_errors=1, header=true)